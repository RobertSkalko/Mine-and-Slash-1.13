package com.robertx22;

public class MyParticle {
/*
    public static class mybigparticle extends Particle {
        protected mybigparticle(World worldIn, double posXIn, double posYIn,
                                double posZIn) {
            super(worldIn, posXIn, posYIn, posZIn);

        }

    }

    protected MyParticle(ResourceLocation resourceLocationIn, boolean alwaysRender) {
        super(resourceLocationIn, alwaysRender);
        //ParticleManager

    }

    @OnlyIn(Dist.CLIENT)
    public static class AmbientMobFactory implements IParticleFactory<BasicParticleType> {
        public AmbientMobFactory() {
        }

        public Particle makeParticle(BasicParticleType typeIn, World worldIn, double x,
                                     double y, double z, double xSpeed, double ySpeed,
                                     double zSpeed) {
            Particle particle = new ParticleSpell(worldIn, x, y, z, xSpeed, ySpeed, zSpeed);
            particle.setAlphaF(0.15F);
            particle.setColor((float) xSpeed, (float) ySpeed, (float) zSpeed);
            return particle;
        }
    }

 */
}
